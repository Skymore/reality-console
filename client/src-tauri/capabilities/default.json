{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the Reality Client main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

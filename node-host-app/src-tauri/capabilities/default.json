{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Node Host packaging shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
